<%= language %>:
  # model and attributes name
  activerecord: &default
    models:
      <%= file_name %>:
        one:
        other:

    attributes:
      <%= file_name %>:

    errors:
      models:
        <%= file_name %>:
          attributes:
  activemodel: *default

  # Enums
  enums:
    <%= file_name %>:

  # AASM state machine
  aasm_state:
    <%= file_name %>:
