<%= language %>:
  # model and attributes name
  activerecord: &default
    models:
      <%= file_path %>:
        one:
        other:

    attributes:
      <%= file_path %>:

    errors:
      models:
        <%= file_path %>:
          attributes:
  activemodel: *default

  # Enums
  enums:
    <%= file_path %>:

  # AASM state machine
  aasm_state:
    <%= file_path %>:
