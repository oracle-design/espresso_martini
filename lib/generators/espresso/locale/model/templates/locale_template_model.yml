<%= language %>:
  activerecord:
    models:
      <%= file_name %>:
        one:
        other:

    attributes:
      <%= file_name %>:

    errors:
      models:
        <%= file_name %>:
          attributes:

  enums:
    <%= file_name %>:
